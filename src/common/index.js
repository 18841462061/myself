const url = 'http://127.0.0.1:3000'

const banner = url + '/banner'

const djprogram = url + '/personalized/djprogram'

const hotList = url + '/personalized'

const hot= url + '/search/hot'

const search = url + '/search'

const song = url + '/song/url'

const list = url + '/toplist/detail'

// const musiclists = url + '/playlist/detail'

// const music = url + '/song/url'

export default {
    banner,
    djprogram,
    hotList,
    hot,
    search,
    song,
    list,
    // musiclists,
    // music

}